import net.rfc1149.harassme
import org.scalatest.matchers.ShouldMatchers
import org.scalatest.Spec  

class Specs extends Spec with ShouldMatchers {
  describe("a spec") {
    it("should do something") {
    }
  }
}